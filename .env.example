# Local SIH demonstration only. Production has stricter fail-closed startup gates.
PORT=4173
NODE_ENV=development
MAAPSURE_DEMO_MODE=true
JWT_SECRET=maapsure-local-demo-secret-change-before-production
PUBLIC_BASE_URL=http://localhost:4173/

# Optional isolated runtime location. Keep this outside source control.
# MAAPSURE_DATA_DIR=/approved/protected/location/maapsure
# ALLOW_SYNTHETIC_DEMO=true
